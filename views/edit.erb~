<br/><b>Edit URL Entry:</a></b><br/>
<div class="entryContainer">

	  <form name="editurlform" action="/update/<%= @url.id %>" method="POST">

	  	<table width="100%">
	  		<tr>
	  			<td align="right">
	  				Link: 
	  			</td>
	  			<td>
					<input id="url_url" name="url_url"  value="<%= @url.url %>">
	  			</td>
	  			<td align="right">
					<a 
						href="#" 
						class="btn goSave" 
						onclick="document.editurlform.submit();"
					>Save
					</a>
	  			</td>
  			</tr>
  			<tr>
	  			<td align="right">
	  				Description: 
	  			</td>
	  			<td>
					<input id="url_desc" name="url_desc"  value="<%= @url.description %>"><br>
	  			</td>
	  			<td align="right">
					<a 
						href="#" 
						class="btn goCancel" 
						onclick="window.location.href='/';return false;"
					>Cancel
					</a> 
	  			</td>
  			</tr>
		</table>

		<br/>
		<br/>

		<fieldset>
			<legend>Categories: <a href="/cats/">Edit</a></legend>

			<% @categories.each do |cat| %>
				<input 
					id="category_id_<%= cat.id %>" 
					name="category_id"
					type="checkbox" 
					value="<%= cat.id %>" 
					<%= "CHECKED" if Url.count(:category_id => cat.id, :id => @url.id) > 0 %>
				/>
				<label for="<%= cat.id %>"><%= cat.name %></label> 

			<% end %>
		</fieldset>

	</form>
</div>

<br/>
	<div class="goInfo">
		<div class="sdate">Created on <%= @url.created_at.strftime("%B %d, %Y at %I:%M %p") %></div>
		<div class="sdate">Updated on <%= @url.updated_at.strftime("%B %d, %Y at %I:%M %p") %></div>
	</div>	
  <br/><a href="/">Back</a>

